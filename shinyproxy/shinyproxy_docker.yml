# Use root/example as user/password credentials
version: '3.5'

services:
                              
  shinyproxy:
#    image: openanalytics/shinyproxy:latest
    image: openanalytics/shinyproxy:2.6.1
    ports:
      - 8080:8080
    networks:
      - sp-net
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      placement:
        constraints:
          - node.role==manager
    volumes:
      - ./application/application_test.yml:/opt/shinyproxy/application.yml
      - /var/run/docker.sock:/var/run/docker.sock
      - ./templates/test/:/templates/test/
    user: ":${DOCKERID}"
    
    
  dburg:
    image: mariadb
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: coucouurg
      MYSQL_DATABASE: mydburg
      MYSQL_USER: userurg
      MYSQL_PASSWORD: userurg
    networks:
        - sp-net

networks:
  sp-net:
    external: true

